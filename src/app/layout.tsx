import type { Metadata } from "next";
import { Space_Grotesk, DM_Sans } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

const dmSans = DM_Sans({
  variable: "--font-dm-sans",
  subsets: ["latin"],
  weight: ["400", "500", "700"],
});

export const metadata: Metadata = {
  title: "Sahil Bajaj | SDE x Product Manager",
  description:
    "Ex-Amazon SDE turned Product Manager. Building at the intersection of engineering and product strategy.",
  keywords: ["Sahil Bajaj", "Product Manager", "Software Engineer", "Amazon", "AWS", "Portfolio"],
  openGraph: {
    title: "Sahil Bajaj | SDE x Product Manager",
    description: "Ex-Amazon SDE turned Product Manager.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${spaceGrotesk.variable} ${dmSans.variable} antialiased`}
    >
      <body className="min-h-screen bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
